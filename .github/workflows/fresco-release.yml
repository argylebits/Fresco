name: Fresco (release)

on:
  workflow_run:
    workflows: ["Release"]
    types: [completed]

permissions:
  contents: write
  pull-requests: write

jobs:
  fresco:
    if: github.event.workflow_run.conclusion == 'success'
    uses: ./.github/workflows/fresco.yml
    secrets: inherit
